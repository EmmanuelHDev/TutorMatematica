<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <!-- Campo de texto para ingresar texto a convertir en voz -->
        <EditText
            android:id="@+id/editTextTextToSpeech"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Escribe algo aquí"
            android:padding="16dp"
            android:layout_marginBottom="16dp"
            android:layout_alignParentTop="true" />

        <!-- Botón para convertir texto a voz -->
        <Button
            android:id="@+id/btnTextToSpeech"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Convertir a Voz"
            android:layout_below="@id/editTextTextToSpeech"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="16dp"/>

        <!-- TextView para mostrar el resultado de reconocimiento de voz a texto -->
        <TextView
            android:id="@+id/textViewSpeechToText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Resultado de voz a texto aparecerá aquí"
            android:layout_below="@id/btnTextToSpeech"
            android:layout_marginBottom="16dp"
            android:layout_centerHorizontal="true"
            android:textSize="18sp" />

        <!-- Botón para iniciar reconocimiento de voz -->
        <Button
            android:id="@+id/btnSpeechToText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Reconocer Voz"
            android:layout_below="@id/textViewSpeechToText"
            android:layout_centerHorizontal="true" />
    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>