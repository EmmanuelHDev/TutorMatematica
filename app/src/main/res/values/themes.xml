<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TutorMatematica" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TutorMatematica" parent="Base.Theme.TutorMatematica" />
    <style name="SplashTheme" parent="Theme.TutorMatematica">
        <item name="android:windowBackground">@color/amarillo</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ai</item>
        <item name="windowSplashScreenAnimationDuration">100</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.TutorMatematica</item>
    </style>
</resources>